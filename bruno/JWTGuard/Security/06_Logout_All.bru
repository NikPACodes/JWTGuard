meta {
  name: 06 Logout All
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/auth/logout-all/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}