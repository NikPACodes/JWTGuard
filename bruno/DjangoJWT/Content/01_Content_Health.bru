meta {
  name: 01 Content Health
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/content/health/
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}